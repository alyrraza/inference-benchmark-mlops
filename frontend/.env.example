# Copy to .env.local to override - both have working defaults baked into
# the code (see src/api.js and src/components/GrafanaEmbed.jsx), so this
# file is optional, not required to run the app.

VITE_API_BASE=http://127.0.0.1:8000
VITE_GRAFANA_URL=http://127.0.0.1:3000
